@model WebApplication1.Models.Student

@{
    ViewData["Title"] = $"Delete Student: {Model.Name}";
}

<div class="p-4 bg-white rounded-lg shadow-sm">
    <h1 class="text-danger">Delete Student</h1>
    
    <h4 class="mt-4">Are you absolutely sure you want to delete this student?</h4>
    <p class="alert alert-warning">
        **Warning:** Deleting the student **@Model.Name** cannot be undone.
    </p>

    <div class="card card-body bg-light">
        <dl class="row">
            <dt class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.StudentId)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.StudentId)
            </dd>
            
            <dt class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Name)
            </dd>

            <dt class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.Address)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Address)
            </dd>

            <dt class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.Grade)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Grade)
            </dd>
            
            <dt class="col-sm-3 fw-bold">
                Department ID
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.DeptId)
            </dd>
        </dl>
    </div>

    <hr/>
    <form asp-action="Delete" id="deleteForm" method="post" class="mt-3">
        @* The hidden input is for the primary key *@
        <input type="hidden" asp-for="StudentId" />
        
        <button type="submit" class="btn btn-danger shadow-sm">
            <i class="bi bi-trash"></i> Delete Student
        </button>
        
        <a asp-action="Index" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Back to List
        </a>
    </form>
</div>

@section Scripts {
    <script>
        // Get the form element
        var deleteForm = document.getElementById('deleteForm');

        // Add an event listener to the form submission
        deleteForm.addEventListener('submit', function(event) {
            // Display the confirmation dialog, customized with the student's name
            var confirmation = confirm("Are you absolutely sure you want to delete the student '@Model.Name'? This action cannot be undone.");

            // If the user clicks 'Cancel', prevent the form submission
            if (!confirmation) {
                event.preventDefault();
                console.log("Deletion cancelled by user.");
            }
        });
    </script>
}